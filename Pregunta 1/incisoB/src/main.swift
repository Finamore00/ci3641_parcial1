import Foundation
import functions

/*
Pequeño cliente para dar al usuario a escoger el programa a ejecutar y llevar a cabo
las pruebas unitarias
*/

@main class Client {
    static func main() {
        let s = "hola"
        print(StringRotate(s, 5))
    }
}